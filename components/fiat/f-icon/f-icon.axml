<import-sjs name="element" from="../../../utils/element.sjs"/>

<view class="{{ element.classAttr('f-icon', className) }}"
  style="background-image: url('components/fiat/assets/{{ name }}.svg'); width: {{ w ? (w + 'px') : '100%' }}; height: {{ h ? (h + 'px') : '100%' }}"
  onTap="onTap">
  <slot/>
</view>