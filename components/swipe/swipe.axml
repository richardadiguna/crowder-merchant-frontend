<import-sjs name="element" from="../../utils/element.sjs"/>

<view class="{{ className }}"
  animation="{{ swipeAnimation ? swipeAnimation : derivedAnimation }}"
  onTouchStart="onTouchStart"
  catchTouchMove="onTouchMove"
  onTouchEnd="onTouchEnd">
  <slot/>
</view>